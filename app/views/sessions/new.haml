.m-4
  %h1= "Login"

  - flash.each do |type, message|
    - cls = type == 'notice' ? 'alert-info' : 'alert-danger'
    .alert{ class: cls, role: :alert }
      = message

  %h2= "Select login method"

  = button_to "Log in with Google", './auth/google_oauth2', class: "btn btn-outline-secondary"
